export const app = {
  dock: {
    setIcon: (): void => {}
  }
}

export class BrowserWindow {}

export const ipcMain = {
  handle: (): void => {},
  on: (): void => {}
}

export const shell = {
  openExternal: (): void => {}
}

export default {
  app,
  BrowserWindow,
  ipcMain,
  shell
}
